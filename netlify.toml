[build]
  command = "npm install && npx prisma generate && (npx prisma db push || true) && (npm run db:seed || true) && npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
